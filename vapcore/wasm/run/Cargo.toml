[package]
description = "Vapcore WASM Test Run"
name = "vapcore-wasm-run-test"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io>", "Tetcoin Developers <support@tetcoin.org>"]
publish = false

[dependencies]
serde = "1"
serde_json = "1"
serde_derive = "1"
vapory-types = "0.8.0"
vapjson = { path = "../../../json" }
tetsy-vm = { version = "0.1.0", path = "../../vm" }
vapcore-wasm = { version = "0.1.0", path = "../" }
clap = "2.24"
env_logger = "0.5"
rustc-hex = "1"

[features]
default = ["vapory-types/std"]
